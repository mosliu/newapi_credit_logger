# NewAPI Key 余额监控系统开发计划

## 1. 项目目标

构建一个可配置、可追踪、可扩展的余额监控系统，支持：

- 管理后台配置多个 NewAPI Key 与地址。
- 按配置周期（默认 300 秒 / 5 分钟）定时拉取余额并记录。
- 前端页面查询监控对象与余额历史记录。
- 数据库同时支持 SQLite、MySQL、PostgreSQL。
- 日志按文件落盘，满足运维排障。
- 支持 Docker 部署与分发（镜像构建、启动编排、版本发布说明）。

## 2. 已确认需求范围

- 监控源配置：`base_url`、`api_key`、`interval_seconds`、`enabled`。
- 监控源说明字段：`key_owner`、`remark`。
- 监控源业务字段：`key_account`、`customer_info`、`key_created_at`、`fee_amount`、`fee_currency`。
- 定时采集：默认 5 分钟，可按监控源配置自定义。
- 记录结果：成功/失败都要入库。
- 查询展示：列表 + 历史趋势 + 失败记录 + 普通用户 Key 片段查询（前缀/后缀）。
- 日志：使用 `loguru`，记录到项目日志目录。
- 交付 Docker 部署与分发能力：`Dockerfile`、`docker-compose`、镜像使用文档。

## 3. 技术方案

- Web/API：FastAPI
- ORM/迁移：SQLAlchemy 2.x + Alembic
- 调度：APScheduler
- HTTP 客户端：httpx
- 日志：loguru
- 前端：服务端渲染管理页（后续可拆前后端）
- 包管理：uv

## 4. 分阶段任务

### 阶段 A：工程骨架与基础设施

- [x] 建立目录结构（`app/`、`docs/`、`logs/`、`migrations/`、`tests/`）。
- [x] 配置系统（`.env` + settings）与多数据库连接字符串。
- [x] 初始化 SQLAlchemy 会话与 Alembic。
- [x] 初始化 loguru（按文件分类、按天切分）。

### 阶段 B：数据库模型与后台配置

- [x] 建表 `api_key_source`（含 `key_owner`、`remark`）。
- [x] 扩展 `api_key_source` 元数据字段（`customer_info`、`key_created_at`、`fee_amount`、`fee_currency`）并接入后台表单。
- [x] 扩展 `api_key_source` 字段 `key_account`（Key 所属账号）并接入 API 与页面展示。
- [x] 建表 `balance_record`（采集结果与错误信息）。
- [x] 实现后台配置 CRUD（新增/编辑/启停/删除）。
- [x] 后台新建页 `api_key` 强制必填校验，并支持“复制新增”快捷入口（除 `api_key` 外自动带入原配置）。
- [x] API Key 存储加密与展示脱敏。

### 阶段 C：定时采集引擎

- [x] 设计 Provider 抽象接口（先实现 NewAPI Provider）。
- [x] 按配置动态注册 APScheduler 任务。
- [x] 执行采集并记录到 `balance_record`。
- [x] 失败重试与错误日志落盘。
- [x] 支持 NewAPI 多版本与 OneAPI 兼容 Provider 选择。
- [x] 调整旧版 NewAPI 为优先使用 `subscription + usage` 路径组合。
- [x] 统一金额单位为元并保留 2 位小数，每次采集同时记录额度与使用量。
- [x] 补充项目 README，覆盖安装、启动、Provider、日志、后台登录与数据库说明。

### 阶段 D：查询与可视化页面

- [x] 监控列表页（最新余额、状态、最近采集时间）。
- [x] 详情页（时间筛选、趋势图、失败明细）。
- [x] 支持按 `key_owner` 筛选。
- [x] 后台管理页增加登录密码保护。
- [x] 管理后台与前台页面按 `docs/GUI_SPECIFICATION.md` 完成统一视觉升级。
- [x] 新增普通用户 Key 片段查询页（无需登录，支持前缀/后缀 12 位+ 匹配）。
- [x] 新增普通用户首页 `/ui`，支持多 Tab 切换与无登录查询入口。
- [x] 将 `html_api_test` 功能完整复制到首页工具 Tab（规则解析、LLM 解析、API 测试、API Key 查询与 CSV 导出）。
- [x] 将首页工具区二级 Tab 扁平化为一级 Tab（`API 测试`、`API Key 查询`）。
- [x] 在 `/ui` 与 `/admin` 顶栏增加版本号显示（统一由配置驱动）。

### 阶段 E：稳定性与交付预留

- [x] 健康检查与就绪检查接口。
- [x] 核心链路测试（优先 SQLite，兼容 MySQL/PostgreSQL）。
- [x] 实现 `Dockerfile`（生产镜像构建、最小运行依赖）。
- [x] 实现 `docker-compose.yml`（服务启动、数据库连接、日志卷挂载）。
- [x] 编写镜像分发文档（版本标签规范、构建与发布命令、回滚说明）。
- [x] 增强上游请求调试日志，默认日志级别调整为 `DEBUG`。
- [x] 补齐后台会话中间件缺失依赖 `itsdangerous`。
- [x] 修复后台登录路由返回类型注解导致的 FastAPI 启动错误。
- [x] 修复后台认证中间件先于 SessionMiddleware 执行导致的 500 错误。
- [x] 增强上游非 JSON 响应排查日志，并同步输出到控制台。
- [x] 为所有上游请求统一设置固定 `User-Agent` 请求头。
- [x] 补充 MySQL/PostgreSQL 自动建表 SQL（Alembic 离线 `--sql` 输出）与数据库初始化文档。
- [x] 新增发布日志文档 `docs/RELEASE_NOTES.md` 并维护版本发布记录。

## 5. 验收标准（DoD）

- [ ] 三种数据库下可正常启动并完成采集。
- [ ] 配置一个监控源后，按间隔持续生成余额记录。
- [ ] 管理后台可编辑 `key_owner`、`key_account`、`remark`、`customer_info`、`key_created_at`、`fee_amount`、`fee_currency` 并可查询展示。
- [ ] 普通用户无需登录即可通过 Key 片段查询到匹配监控源（仅展示脱敏 Key）。
- [ ] 日志可定位请求、耗时、错误原因。
- [ ] 核心功能具备自动化测试。
- [ ] Docker 镜像可独立启动，`docker-compose` 可完成一键部署。
- [ ] 提供可执行的镜像分发与版本管理说明。

## 6. 进度实时更新规则

- 每完成一个子任务，必须立即更新本文件：
  - 勾选对应任务 `[x]`
  - 在“进度日志”追加一条记录
  - 若变更范围或优先级变化，在“计划变更记录”追加说明
- 严禁只口头汇报而不更新本文件。

## 7. 当前进度看板

| 阶段 | 状态 | 完成度 | 备注 |
| --- | --- | --- | --- |
| A 工程骨架与基础设施 | 已完成 | 100% | 已完成基础可运行骨架、配置、日志与 Alembic 初始化 |
| B 数据库模型与后台配置 | 已完成 | 100% | 已完成模型、迁移、配置 CRUD API、key 加密脱敏，扩展账号/客户/创建时间/收费字段，并支持后台复制新增 |
| C 定时采集引擎 | 已完成 | 100% | 已完成 Provider 抽象、NewAPI 多版本兼容、OneAPI 兼容接入、任务调度与采集落库 |
| D 查询与可视化页面 | 已完成 | 100% | 已完成查询页、独立后台管理页、登录保护、普通用户首页多 Tab（含 Key 查询/API 测试/API Key 查询）与页面版本号显示，并按 GUI 规范升级视觉风格 |
| E 稳定性与交付预留 | 已完成 | 100% | 已完成测试、Docker 交付文件、上游请求调试日志增强、发布日志文档落地，并修复后台登录相关运行时问题；当前环境无 Docker 命令，未执行容器实跑 |

## 8. 进度日志

- 2026-03-07：创建计划文档，冻结第一版范围与阶段拆分。
- 2026-03-07：完成阶段 A，已落地 FastAPI 应用骨架、Settings、多数据库连接、loguru 日志、Alembic 初始化与可用性校验。
- 2026-03-07：完成阶段 B，已落地 `api_key_source`/`balance_record` 模型、首个 Alembic 迁移、配置 CRUD API、API Key 加密存储与脱敏展示。
- 2026-03-07：完成阶段 C，已落地 Provider 抽象、APScheduler 动态任务重载、定时/手动采集与失败重试、采集结果落库与调度日志。
- 2026-03-07：完成阶段 D，已落地 `/ui/sources` 列表页、`/ui/sources/{id}` 详情页、时间筛选、趋势图和失败记录展示。
- 2026-03-07：完成阶段 E，已落地健康检查、自动化测试（2 passed）、Dockerfile、docker-compose 与镜像分发文档；当前环境缺少 Docker 命令，未执行容器运行验证。
- 2026-03-07：新增独立后台管理系统 `/admin`，支持监控源新增、编辑、启停、立即采集、删除与记录查看，并补充后台管理流程自动化测试。
- 2026-03-08：完成多 Provider 版本适配，新增 `newapi_legacy` 与 `oneapi` 可选项；旧版 NewAPI 优先兼容 `credit_grants`，并补充 Provider 兼容说明文档。
- 2026-03-08：根据最新站点验证结果，调整旧版 NewAPI 余额采集策略，改为优先使用 `/v1/dashboard/billing/subscription` 与 `/v1/dashboard/billing/usage` 组合计算余额。
- 2026-03-08：按最新计量规则调整金额处理：`subscription` 按元、`usage` 按分换算为元；新增 `limit_amount` 与 `usage_amount` 落库字段，余额统一保留 2 位小数，并已重建 SQLite 数据库。
- 2026-03-08：补充 `README.md`，统一项目使用说明、环境变量、Provider 策略、日志文件与数据库重建方式。
- 2026-03-08：后台新增密码登录与会话鉴权，未登录访问 `/admin` 会重定向到 `/admin/login`。
- 2026-03-08：增强请求上游站点的调试日志，新增 `upstream` 组件日志文件，并将默认日志级别调整为 `DEBUG` 以便排查兼容问题。
- 2026-03-08：修复后台登录启动失败问题，补齐 `SessionMiddleware` 所需依赖 `itsdangerous`。
- 2026-03-08：修复后台登录路由启动错误，将 `HTMLResponse | RedirectResponse` 联合返回注解改为通用 `Response`，避免 FastAPI 将其误判为响应模型。
- 2026-03-08：修复 `/admin` 访问 500 问题，调整中间件注册顺序，确保 `SessionMiddleware` 先于后台认证中间件生效，并为会话访问增加防御式判断。
- 2026-03-08：增强上游请求排障能力，新增控制台日志输出，并在响应非 JSON 时记录 `content-type`、响应片段与更清晰的错误信息。
- 2026-03-08：为所有上游 HTTP 请求统一增加固定 `User-Agent` 头，减少目标站点按默认客户端特征返回异常页面的概率。
- 2026-03-09：扩展监控源元数据字段，新增 `customer_info`、`key_created_at`、`fee_amount`、`fee_currency`，并完成 API/后台表单/列表展示与测试覆盖。
- 2026-03-09：新增 `key_account` 字段并完成数据库迁移、API Schema、后台表单、查询页展示及测试覆盖。
- 2026-03-09：按 `docs/GUI_SPECIFICATION.md` 对 `/admin` 与 `/ui` 模板进行统一美化，落地玻璃拟态、细边框、层次阴影与页面入场动效。
- 2026-03-09：更新 `AGENTS.md`，新增“页面设计必须遵循 `docs/GUI_SPECIFICATION.md`”强制约束。
- 2026-03-09：测试环境默认切换到独立 SQLite 测试库，并在用例启动时重建 schema，避免测试影响业务库结构与数据。
- 2026-03-09：后台新建页增加 `api_key` 必填校验与显式标识；监控源列表新增“复制新增”按钮，自动预填除 `api_key` 外的字段。
- 2026-03-09：新增普通用户 Key 片段查询页 `/ui/key-search`，支持前缀/后缀 12 位+ 与片段匹配，并仅展示脱敏 Key。
- 2026-03-09：调整普通用户 Key 片段查询结果字段，聚焦展示名称、匹配方式、脱敏 Key、Owner、最新额度/余额/使用量。
- 2026-03-09：新增普通用户首页 `/ui` 多 Tab 布局，默认直达 Key 片段查询；并将 `html_api_test` 的 API 测试与 API Key 查询能力完整复制到首页工具 Tab，含规则解析、LLM 解析、联通测试、CSV 导出与令牌信息复制。
- 2026-03-09：补充自动化回归测试，新增 `/ui` 首页可访问性与 `/api/config` 返回结构校验，确保首页工具能力持续可用。
- 2026-03-09：按最新交互调整，将“API 可用性工具”下二级页提升为首页一级 Tab，形成 `Key 片段查询`、`API 测试`、`API Key 查询` 三个并列入口，并兼容旧参数 `tab=api-tools`。
- 2026-03-09：新增 `APP_VERSION` 配置并接入页面模板，在 `/ui` 与 `/admin` 顶栏显示版本号；同步将应用运行版本统一为配置驱动。
- 2026-03-09：新增发布日志文档 `docs/RELEASE_NOTES.md`，补充 `v0.1.1` 发布说明，并在 README 增加入口。
- 2026-03-10：补充 MySQL/PostgreSQL 的“自动建表”说明文档，并提供 Alembic 离线生成的建表 SQL 文件，便于手工执行或审计。
- 2026-03-10：调整默认采集周期为 5 分钟（`DEFAULT_POLL_INTERVAL_SECONDS=300`），普通用户 Key 片段查询匹配阈值提升为 12 位+，并收敛普通用户顶栏入口为 3 个功能按钮（Key/API/API Key）；同时为 API Key 查询增加 NewAPI 新版/老版选择并走不同查询逻辑。

## 9. 计划变更记录

- 2026-03-07：在原 SQLite/MySQL 基础上新增 PostgreSQL 支持。
- 2026-03-07：监控源字段明确包含 `key_owner` 与 `remark`。
- 2026-03-07：将 Docker 从“预留”升级为“必须交付部署与分发能力”。
- 2026-03-07：补充“独立后台管理系统”增量需求，管理入口统一为 `/admin`。
- 2026-03-08：新增“站点版本/系统类型可选”增量需求，监控源定义支持区分新版 NewAPI、旧版 NewAPI 与 OneAPI 兼容接口。
- 2026-03-08：修正旧版 NewAPI 兼容策略，优先路径从 `credit_grants` 调整为 `subscription + usage`。
- 2026-03-08：补充“采集记录需同时保存额度与使用量，且统一按元保留 2 位小数”增量需求，并按确认执行数据库重建。
- 2026-03-08：补充“交付可直接使用的 README 文档”增量需求，集中收敛项目说明。
- 2026-03-08：新增“后台需要登录密码”增量需求，采用会话 Cookie 保护整个 `/admin` 路由组。
- 2026-03-08：新增“上游请求调试日志增强”增量需求，默认日志级别切换为 `DEBUG`。
- 2026-03-08：补充后台登录会话依赖声明，避免运行时缺少 `itsdangerous`。
- 2026-03-08：修正后台登录路由类型注解，避免 FastAPI 在启动时为响应类联合类型生成无效响应模型。
- 2026-03-08：修正后台认证中间件与会话中间件执行顺序，避免未注入 session 时访问 `request.session`。
- 2026-03-08：补充“控制台可见的上游请求调试日志”与“非 JSON 响应清晰报错”增量需求，提升兼容站点排查效率。
- 2026-03-08：补充“所有上游请求统一设置固定 User-Agent”增量需求，避免部分站点对默认客户端返回非预期内容。
- 2026-03-09：新增“监控源需登记客户信息、业务创建时间、收费信息”增量需求，扩展 `api_key_source` 字段与后台管理表单。
- 2026-03-09：新增“监控源需登记 Key 所属账号（`key_account`）”增量需求，并要求在管理/查询页面可见。
- 2026-03-09：新增“页面设计改造必须遵循 `docs/GUI_SPECIFICATION.md`”执行约束，并同步写入仓库级 `AGENTS.md`。
- 2026-03-09：补充“测试默认使用独立数据库”执行约束，隔离测试与业务数据。
- 2026-03-09：新增“后台新建页 `api_key` 必填 + 列表支持复制新增”的交互需求，复用现有新建流程完成二次创建。
- 2026-03-09：新增“普通用户可按 Key 片段查询（前缀/后缀）”需求，查询结果仅允许展示脱敏 Key。
- 2026-03-09：补充“普通用户 Key 片段查询结果字段固定为关键余额指标”需求，避免暴露非必要配置字段。
- 2026-03-09：新增“创建普通用户首页并支持多 Tab 切换，且将 `html_api_test` 功能完整复制到首页 Tab”需求，并要求页面设计继续遵循 `docs/GUI_SPECIFICATION.md`。
- 2026-03-09：新增“API 可用性工具内部二级 Tab 需提升为首页一级 Tab”交互需求，要求保留旧 `tab` 参数兼容性。
- 2026-03-09：新增“页面显示版本号 + 编写发布日志文档”的交付需求，版本信息统一使用配置项驱动。
- 2026-03-10：新增“提供 MySQL/PostgreSQL 建表 SQL（可直接执行）与初始化文档”的交付需求，建表语句以 Alembic 迁移为唯一来源。
- 2026-03-10：调整默认采集周期为 5 分钟，并提高普通用户 Key 片段查询安全阈值；普通用户页面入口收敛为 3 个功能按钮（Key/API/API Key），监控列表页默认不对普通用户开放；API Key 查询支持 NewAPI 新版/老版切换。
